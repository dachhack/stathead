name: Publish stathead to PyPI

# Publishes the Python package. Two ways to trigger:
#
#   A. Push a tag matching py-v* (e.g. py-v0.1.0). The tag version is
#      strictly verified against python/pyproject.toml.
#
#   B. Run manually from the Actions UI (workflow_dispatch). The version
#      from pyproject.toml is used as-is — no tag needed. Friendly for
#      mobile / quick releases. Choose the branch to publish from.
#
# Uses PyPI trusted publishing (OIDC) — no API token stored in GitHub.
# One-time setup on pypi.org (do this BEFORE the first release):
#   1. Register the project name `stathead` (claim via a first upload;
#      PyPI auto-creates it on publish).
#   2. pypi.org → Your account → Publishing → Add a new pending publisher
#      PyPI Project name: stathead
#      Owner:             dachhack
#      Repo:              stathead
#      Workflow:          python-publish.yml
#      Environment:       pypi
#   3. Repo Settings → Environments → New environment `pypi`
#      (optional: require a reviewer to approve)
#
# Subsequent releases:
#   1. Bump `version` in python/pyproject.toml
#   2. Bump `__version__` in python/src/stathead/__init__.py
#   3. Either: tag py-v0.2.0 + push, OR run workflow_dispatch from the UI,
#      OR push the release commit to the `publish-python-now` branch:
#        git push -f origin <commit>:publish-python-now
#      (branch-push rights only — lets a Claude Code session release, whose
#      git proxy refuses tag pushes and whose GitHub app cannot dispatch.
#      Guarded: the commit must already be on the default branch.)

on:
  push:
    tags:
      - 'py-v*'
    branches: [publish-python-now]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0   # merge-base for the publish-python-now guard below
      # Branch-push trigger safety: only code already merged to the default
      # branch may publish this way.
      - name: Require the commit to be on the default branch (publish-python-now only)
        if: github.ref == 'refs/heads/publish-python-now'
        run: |
          git fetch --no-tags origin claude/nfl-fantasy-workbench-6D1yd
          if ! git merge-base --is-ancestor HEAD FETCH_HEAD; then
            echo "::error::$(git rev-parse --short HEAD) is not on claude/nfl-fantasy-workbench-6D1yd — merge first, then push that commit to publish-python-now."
            exit 1
          fi
          echo "OK: $(git rev-parse --short HEAD) is on claude/nfl-fantasy-workbench-6D1yd."
      - uses: actions/setup-python@v6
        with:
          python-version: '3.11'
          cache: pip
      - name: Install + test
        working-directory: python
        run: |
          pip install -e ".[dev]"
          pytest -q

  build:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v6
        with:
          python-version: '3.11'
      - name: Install build
        run: pip install build
      - name: Build sdist + wheel
        working-directory: python
        run: python -m build
      - name: Verify tag matches pyproject version (tag-triggered only)
        if: startsWith(github.ref, 'refs/tags/py-v')
        working-directory: python
        run: |
          TAG="${GITHUB_REF_NAME#py-v}"
          PKG_VER=$(python -c "import tomllib,pathlib; print(tomllib.loads(pathlib.Path('pyproject.toml').read_text())['project']['version'])")
          if [ "$TAG" != "$PKG_VER" ]; then
            echo "::error::Tag $GITHUB_REF_NAME => $TAG does not match pyproject version $PKG_VER"
            exit 1
          fi
      - name: Show pyproject version (workflow_dispatch / publish-python-now)
        if: github.event_name == 'workflow_dispatch' || github.ref == 'refs/heads/publish-python-now'
        working-directory: python
        run: |
          PKG_VER=$(python -c "import tomllib,pathlib; print(tomllib.loads(pathlib.Path('pyproject.toml').read_text())['project']['version'])")
          echo "Publishing stathead $PKG_VER from ${GITHUB_REF_NAME}" >> "$GITHUB_STEP_SUMMARY"
      - uses: actions/upload-artifact@v6
        with:
          name: dist
          path: python/dist/

  publish:
    needs: build
    runs-on: ubuntu-latest
    environment: pypi
    permissions:
      id-token: write  # required for PyPI trusted publishing
    steps:
      - uses: actions/download-artifact@v8
        with:
          name: dist
          path: dist/
      - uses: pypa/gh-action-pypi-publish@release/v1

